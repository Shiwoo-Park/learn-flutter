import 'package:flutter/material.dart';

class ClimateSealevelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('ClimateSealevelScreen'),
      ),
    );
  }
}
